# 이 저장소에서 일하는 방식

`index.html` 하나가 앱 전부입니다. 빌드 과정이 없고, 인라인 스크립트·스타일은 CSP 해시로
고정되어 있습니다. 자세한 구조와 배포는 `README.md`를 보세요.

## 변경 절차 — `main`에서 바로, 물어보지 말고 끝까지

**작업 브랜치도 PR도 머지도 쓰지 않습니다.** `main`에서 직접 고치고 밀어 넣습니다. 사용자가
원하는 결과물은 "머지해도 되는 PR"이 아니라 **사이트에 반영된 변경**입니다. 아래를 사용자
확인 없이 끝까지 진행하세요.

1. `git checkout main && git pull origin main` 으로 최신 상태에서 시작합니다.
2. 고칩니다. 인라인 `<script>`·`<style>`을 한 글자라도 건드렸으면 `npm run csp`로 해시를
   다시 만듭니다.
3. **밀기 전에 `npm test`를 로컬에서 돌립니다**(CSP 해시 검사 → `html-validate` → 실제
   Chromium 검증). 하나라도 실패하면 밀지 않습니다. 여기가 유일한 사전 관문입니다.
4. 커밋하고 `git push origin main`.
5. CI(`verify`)를 지켜봅니다. 빨간불이면 **그 자리에서** 원인을 고쳐 다시 밉니다.
6. `deploy` 잡이 GitHub Pages에 올린 것까지 확인하고, 결과를 한 번에 보고합니다.

### 빨간불이 나도 사이트는 안전합니다

`deploy`는 `needs: verify`라 **검증을 통과한 커밋만 배포됩니다.** 잘못된 커밋이 `main`에
올라가도 배포는 멈추고 사이트는 직전 버전을 그대로 서비스합니다. 그래도 `main`은 항상 초록이어야
하므로, 빨간불을 남겨 두고 세션을 끝내지 마세요.

### 되돌리기

지운 이력을 다시 쓰지 않습니다. `git revert <sha>`로 되돌리는 커밋을 만들어 밀면, 같은 절차로
검증·배포가 다시 돕니다. 강제 푸시(`--force`)는 쓰지 않습니다.

### 그래도 멈추고 묻는 경우

아래에 해당하면 밀기 전에 사용자에게 묻습니다. 그 외에는 묻지 않습니다.

- **되돌리기 어려운 변경** — 저장된 대화 데이터의 형식 변경, 저장소 설정, 공개 URL 변경
- 사용자가 방금 요청한 **범위를 넘어서는** 변경
- 사용자가 **검토를 원한다고 말한** 변경 — 그때만 브랜치와 PR을 씁니다

### 사용자가 자리에 없을 때

밀어 놓고 세션이 유휴 상태가 되면 안 됩니다. CI와 배포가 끝날 때까지 확인하고, 오래 걸리면
자기 점검(`send_later`)을 걸어 둡니다. 상태가 그대로면 조용히 다음 점검만 다시 겁니다.

## 지켜야 할 불변 사항

- **가져온 대화는 서버로 나가지 않습니다.** `connect-src 'none'`을 비롯한 `<meta>` CSP를
  약하게 만들지 마세요. `npm test`가 문서 자신과 `blob:`/`data:` 외의 요청을 한 건이라도
  감지하면 실패합니다.
- **디자인은 현재 모습을 유지합니다.** 레이아웃·색·간격은 버그를 고칠 때만 건드립니다.
- 새 동작을 넣거나 버그를 고치면 `tests/verify.mjs`에 검사를 추가합니다. 회귀를 실제로
  잡는지(고치기 전 코드에서 실패하는지) 확인하고 넣으세요. PR 리뷰가 없는 만큼 이 스위트가
  유일한 안전망입니다.
