# 이 저장소에서 일하는 방식

`index.html` 하나가 앱 전부입니다. 빌드 과정이 없고, 인라인 스크립트·스타일은 CSP 해시로
고정되어 있습니다. 자세한 구조와 배포는 `README.md`를 보세요.

## 변경 절차 — 물어보지 말고 끝까지 진행할 것

작업 요청을 받으면 **아래 단계를 사용자 확인 없이 끝까지** 진행합니다. 특히 머지는 별도
지시를 기다리지 않습니다. 사용자가 원하는 결과물은 "머지해도 되는 PR"이 아니라 **사이트에
반영된 변경**입니다.

1. `claude/*` 브랜치에서 작업하고 커밋합니다.
2. 밀기 전에 로컬에서 `npm test`를 돌립니다
   (CSP 해시 검사 → `html-validate` → 실제 Chromium 검증). 실패하면 밀지 않습니다.
3. 인라인 `<script>`·`<style>`을 한 글자라도 고쳤으면 `npm run csp`로 해시를 다시 만듭니다.
   잊으면 2단계가 먼저 잡습니다.
4. 푸시하고 PR을 엽니다. 무엇을·왜·어떻게 확인했는지 본문에 적습니다.
5. CI(`verify`)를 지켜봅니다. 빨간불이면 원인을 고쳐 다시 밀고, 초록이 될 때까지 반복합니다.
6. **초록이 되면 바로 머지합니다**(merge commit). 그리고 작업 브랜치를 지웁니다.
7. `main`의 `deploy` 잡이 GitHub Pages로 배포한 것까지 확인하고, 그 결과를 사용자에게
   한 번에 보고합니다.

### 멈춰야 하는 경우

아래에 해당하면 6단계에서 멈추고 사용자에게 묻습니다. 그 외에는 묻지 않습니다.

- 사람이 리뷰에서 **변경을 요청**했고 아직 풀리지 않은 경우
- PR에 `hold` 라벨이 붙어 있거나 **초안(draft)** 인 경우
- 되돌리기 어려운 변경 — 저장된 대화 데이터의 형식 변경, 기본 브랜치·저장소 설정 변경,
  공개 URL 변경
- 사용자가 방금 요청한 범위를 **넘어서는** 변경을 하게 되는 경우

### 사용자가 자리에 없을 때

PR을 열어 둔 채 세션이 유휴 상태가 되면 안 됩니다. PR 이벤트를 구독하고
(`subscribe_pr_activity`), 한 시간 간격 자기 점검(`send_later`)을 걸어 두고, 머지·클로즈될
때까지 유지합니다. 상태가 그대로면 조용히 다음 점검만 다시 겁니다.

## 지켜야 할 불변 사항

- **가져온 대화는 서버로 나가지 않습니다.** `connect-src 'none'`을 비롯한 `<meta>` CSP를
  약하게 만들지 마세요. `npm test`가 문서 자신과 `blob:`/`data:` 외의 요청을 한 건이라도
  감지하면 실패합니다.
- **디자인은 현재 모습을 유지합니다.** 레이아웃·색·간격은 버그를 고칠 때만 건드립니다.
- 새 동작을 넣거나 버그를 고치면 `tests/verify.mjs`에 검사를 추가합니다. 회귀를 실제로
  잡는지(고치기 전 코드에서 실패하는지) 확인하고 넣으세요.
