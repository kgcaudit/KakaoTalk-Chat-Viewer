{
  "name": "kakaotalk-chat-viewer",
  "private": true,
  "type": "module",
  "description": "대화서랍 — 브라우저 안에서만 동작하는 카카오톡 대화 뷰어",
  "scripts": {
    "test": "node tools/csp-hashes.mjs --check && npm run lint:html && node tests/verify.mjs",
    "lint:html": "html-validate index.html",
    "csp": "node tools/csp-hashes.mjs",
    "serve": "npx --yes http-server . -p 8080 -c-1"
  },
  "devDependencies": {
    "html-validate": "^10.0.0",
    "playwright": "1.56.1"
  }
}
